@using SODP.UI.ViewModels
@model NewStageVM
<!-- Modal -->
<div class="modal fade" id="editstage" tabindex="-1" role="dialog" aria-labelledby="editStageLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="text-info" id="editStageLabel">@(Model.Stage.Id==0 ? "Nowe" : "Edycja") stadium</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <div class="text-danger" asp-validation-summary="ModelOnly"></div>
                <div id="error-placeholder"></div>
                <form asp-page-handler="NewStage">
                    <input name="IsValidate" type="hidden" value="@ViewData.ModelState.IsValid.ToString()" />
                    <input asp-for="Stage.Id" type="hidden" value="@Model.Stage.Id" />
                    <div class="form-group">
                        <input asp-for="Stage.Sign" onkeyup="this.value = this.value.toUpperCase();" id="sign" class="form-control" value="@Model.Stage.Sign" placeholder="Znak stadium" readonly="@(Model.Stage.Id.Equals(0) ? false : true)" />
                        <span asp-validation-for="Stage.Sign" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <input asp-for="Stage.Title" onkeyup="this.value = this.value.toUpperCase();" id="title" class="form-control" value="@Model.Stage.Title" placeholder="Opis stadium" />
                        <span asp-validation-for="Stage.Title" class="text-danger"></span>
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <partial name="_SubmitAndBackButtonModalPartial" />
            </div>
        </div>
    </div>
</div>