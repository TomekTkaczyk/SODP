@using SODP.UI.ViewModels
@model NewBranchVM
<!-- Modal -->
<div class="modal fade" id="editbranch" tabindex="-1" role="dialog" aria-labelledby="editStageLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="text-info" id="editBranchLabel">Nowa branża</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <div class="text-danger" asp-validation-summary="ModelOnly"></div>
                <div id="error-placeholder"></div>
                <form asp-page-handler="NewBranch">
                    <input name="IsValidate" type="hidden" value="@ViewData.ModelState.IsValid.ToString()" />
                    <input asp-for="Branch.Id" type="hidden" value="@Model.Branch.Id" />
                    <div class="form-group">
                        <input asp-for="Branch.Sign" onkeyup="this.value = this.value.toUpperCase();" id="sign" class="form-control" value="@Model.Branch.Sign" placeholder="Znak branży" readonly="@(Model.Branch.Id.Equals(0) ? false : true)" />
                        <span asp-validation-for="Branch.Sign" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <input asp-for="Branch.Title" onkeyup="this.value = this.value.toUpperCase();" id="title" class="form-control" value="@Model.Branch.Title" placeholder="Opis branży" />
                        <span asp-validation-for="Branch.Title" class="text-danger"></span>
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <partial name="_SubmitAndBackButtonModalPartial" />
            </div>
        </div>
    </div>
</div>
